spring:
  application:
    name: com.zhiyong # 取dataid 如果dataid与服务名不一致需手动修改
  cloud:
    nacos:
      server-addr: 192.168.31.79:8848
      username: nacos
      password: nacos
      #config:
      #  namespace: public #读取配置中心
      #Nacos 客户端 默认是properties的文件扩展名
      #一旦修改了需要重新设置
      #config:
      #  file-extension: yml